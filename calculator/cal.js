function add(a, b) {
  return a + b
}
function sub(a, b) {
  return a - b
}
function mul(a, b) {
  return a * b
}
function div(a, b) {
  return a / b
}
module.exports = { add, sub, mul, div } //use to export multiple functions

// module.exports.add = add  //use to export single function
//move to terminal then change the directory to calculator
//npm init
//name : must be unique
//version : 1.0.0
//entry point : cal.js
