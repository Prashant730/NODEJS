let a = require('./cal') //importing the module cal.js
console.log(a.add(10, 20))
console.log(a.sub(10, 20))
console.log(a.mul(10, 20))
console.log(a.div(10, 20))

//publish the package to npm and after that run the command in terminal

//move to terminal then change the directory to calculator
//npm publish
//after publishing the package we can install it using npm install package-name(prash730)
//then we can use it in our project by importing it -->refer to destop file
//let a = require('prash730')
//console.log(a.add(10,20))
